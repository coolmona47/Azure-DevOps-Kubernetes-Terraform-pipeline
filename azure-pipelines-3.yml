trigger:
- main

resources:
- repo: self

variables:
  tag: 174 
  #'$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
    steps:
    # - task: Docker@2
    #   displayName: Build an image
    #   inputs:
    #     command: build
    #     dockerfile: '$(Build.SourcesDirectory)/Dockerfile'
    #     tags: |
    #       $(tag)
- task: CopyFiles@2
  inputs:
    SourceFolder: '$(System.DefaultWorkingDirectory)'
    Contents: '**/*.yaml'
    TargetFolder: '$(Build.ArtifactStagingDirectory)'
- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    ArtifactName: 'manifests'
    publishLocation: 'Container'

#build stage

#build docker image
#build docker image =>tag174
#copy files to staging directory
#publish
stages:
- stage: Deploy
  displayName: Deploy image
  jobs:
  - job: Deploy
    displayName: Deploy
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: DownloadPipelineArtifact@2
      inputs:
       buildType: 'current'
       artifactName: 'manifests'
       itemPattern: '**/*.yaml'
       targetPath: '$(System.ArtifactDirectory)'
    - task: KubernetesManifest@1
      inputs:
        action: 'deploy'
        connectionType: 'kubernetesServiceConnection'
        kubernetesServiceConnection: 'aws-kubernetes-cluster-service-connection'
        namespace: 'default'
        manifests: '$(System.ArtifactDirectory)/configuration/kubernetes/deployment.yaml'
        containers: mohan3/curreny-exchnage-devops:$(tag)

#deploy stage
#download what is published in the build stage 
#image=> tag174 ->deploy to kubernetes service
#apply- kubectl apply -f deployment.yaml ( whihci isside the kubernted folder in visual stuio code which is deployment.yaml)
